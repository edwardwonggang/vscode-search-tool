Place a Linux x86_64 ctags (universal-ctags recommended) static or musl binary here, named:
  ctags
The extension uploads it to the server as /tmp/ripgreptool-ctags when ctags is not already in PATH.
If you omit this file, the search server must have "ctags" in PATH.
